const testUtils = require('../../lib/testUtils');
const { PaymentSource } = require('../../models/sequelize');
const { PaymentSourceService } = require('./paymentSource');

describe('PaymentSourceService', () => {
  beforeAll(() => {

  })

  afterAll(() => {

  })

  test('getPaymentSourcesByUserId should fetch payment sources of given user', async () => {
    expect.assertions(0);
  });
});
